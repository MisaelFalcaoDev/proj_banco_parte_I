A API que responde a duas requisições. 
-> Um requisição POST que receberá as informações de um usuário e guardará os dados desse usuário. 
-> Uma requisição GET que retornará os dados do usuário, caso ele exista
